sudo: required

language: python

python:
    - '3.6'

branches:
    only:
        - master
        - devel
        - issue_30

before_script:
    - chmod +x scripts/run_scripts.sh
    - npm install
    - npm i -g markdown-to-json
    - pip install -r requirements.txt

cache:
    npm: true
    directories:
        - node_modules

script:
    - ./scripts/run_scripts.sh
    - npm run build

deploy:
    provider: pages
    local_dir: dist/
    skip_cleanup: true
    github_token: e460b3fb8874e670f425b89eafa177776708922e
    on:
      branch: issue_30
